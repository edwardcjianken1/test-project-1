let x = 10; //declares a variable x and assign the value 10 to it
let y = "Hello"; //declares a variable y and assign the value "Hello" to it
let z = x + y; //declares a new variable which contains the result of concatenating x and y

console.log(z); //prints the new variable to the console. The variable contains the result of concatenating x and y
